# 📝 更新日志

本文件记录 Scrapling MCP 面向用户的重要变化。

版本遵循语义化版本号；日期使用 `YYYY-MM-DD`。

## [1.2.0] - 2026-09-11

### ✨ 新增

- 提供 `scrape`、`scrape_batch`、预设登录和自定义登录等 MCP 工具。
- 提供 `scrapling-mcp` / `smcp` CLI，以及状态、Cookie 概况、工具开关、诊断和重启命令。
- 支持 Bilibili、YouTube、GitHub、知乎、微博、小红书预设登录，也支持用户授权的自定义网站登录。
- 支持 Crawl4AI 与 Scrapling 双引擎、自动回退、结构化错误、批量抓取和 CSS 内容选择。
- 增加本机运行日志与脱敏调用日志，便于排查 Agent 调用和服务状态。
- 增加 SSRF 防护、DNS 公网校验、进程树清理、并发/队列/流量限制和上游 HTTP/SOCKS5 代理支持。

### 🔧 改进

- CLI 使用项目相对路径发现 Python 环境，不依赖开发者机器上的固定路径。
- MCP 默认使用 stdio，避免把带有本机登录态的服务暴露到网络端口。
- 登录流程采用“打开窗口 → 用户操作 → 查询状态 → finalize 保存”的显式状态机，减少重复弹窗。
- MCP 返回 `success`、`error_code`、`retryable`、`attempts`、`summary` 等 Agent 友好字段。
- GitHub Actions 在 Windows、Linux、macOS 上运行统一测试，并提高托管 Runner 冷启动容错。

### 🛡️ 安全

- 登录密码、验证码和 Cookie 不通过 MCP 参数传递，也不写入调用日志。
- Cookie 与浏览器状态仅保存到用户本机的受保护数据目录；仓库忽略相关文件。
- 网页内容标记为不可信数据，Agent 不应把网页中的指令当成系统指令或工具授权。

### ⚠️ 已知边界

- 这是公开网页读取与用户授权登录读取工具，不是完整搜索引擎或递归整站爬虫。
- 不保证绕过目标网站的验证码、反爬挑战或第三方登录策略。
- 暂不支持任意用户脚本、文件下载、PDF 解析、POST 页面和递归整站爬取。
- 使用者仍需遵守目标网站的服务条款、robots 规则、隐私和适用法律。

## 发布前验证

- 本地单元测试：`75 passed, 5 skipped`。
- `example.com` 公网冒烟抓取：HTTP 200，Crawl4AI 成功返回正文。
- Bilibili 已保存登录态：可读取登录状态。
- YouTube 和自定义网站：自动化流程已覆盖；实际第三方登录仍受用户交互、代理和站点风控影响。

